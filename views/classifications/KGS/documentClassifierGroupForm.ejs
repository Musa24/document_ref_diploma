<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css" />
    <title>Form</title>
  </head>
  <body>
    <div class="form-container">
      <form
        action="/admin/KGS/documentClassifierGroup/<% if(edit){%><%= documentClassifierGroup.id%>?_method=PUT<%}%>"
        method="POST"
      >
        <h2>Группа классификатора КГС:</h2>
        <label for="group">Группа</label>
        <input
          type="text"
          id="group"
          name="group"
          placeholder="Группа..."
          value="<% if(edit){%><%= documentClassifierGroup.group %><%}%>"
        />

        <label for="groupName">Имя_Группы</label>
        <input
          type="text"
          id="groupName"
          name="groupName"
          placeholder="Имя_Группы..."
          value="<% if(edit){%><%= documentClassifierGroup.groupName %><%}%>"
        />
        <input type="submit" value="Создайте" class="green" />
      </form>
    </div>
  </body>
</html>
